import { Move } from '../../../types/Pokemon';

const cut: Move = {
  id: 'cut',
  name: 'Cut',
  type: 'Normal',
  category: 'Physical',
  power: 50,
  accuracy: 95,
  pp: 30,
  description: 'Cuts using claws, scythes, etc.',
};

export default cut;
