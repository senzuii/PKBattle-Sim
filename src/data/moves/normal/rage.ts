import { Move } from '../../../types/Pokemon';

const rage: Move = {
  id: 'rage',
  name: 'Rage',
  type: 'Normal',
  category: 'Physical',
  power: 20,
  accuracy: 100,
  pp: 20,
  description: 'Raises ATTACK if the user is hit.',
};

export default rage;
