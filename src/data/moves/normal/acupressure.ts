import { Move } from '../../../types/Pokemon';

const acupressure: Move = {
  id: 'acupressure',
  name: 'Acupressure',
  type: 'Normal',
  category: 'Status',
  power: 0,
  accuracy: 100,
  pp: 30,
  description: 'The user applies pressure to stress points, sharply boosting one of its stats.',
};

export default acupressure;
