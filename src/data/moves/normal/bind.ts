import { Move } from '../../../types/Pokemon';

const bind: Move = {
  id: 'bind',
  name: 'Bind',
  type: 'Normal',
  category: 'Physical',
  power: 15,
  accuracy: 85,
  pp: 20,
  description: 'Binds the target for 2-5 turns.',
};

export default bind;
