import { Move } from '../../../types/Pokemon';

const reflecttype: Move = {
  id: 'reflecttype',
  name: 'Reflect Type',
  type: 'Normal',
  category: 'Status',
  power: 0,
  accuracy: 100,
  pp: 15,
  description: 'The user reflects the target’s type, making it the same type as the target.',
};

export default reflecttype;
