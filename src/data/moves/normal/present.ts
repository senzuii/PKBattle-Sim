import { Move } from '../../../types/Pokemon';

const present: Move = {
  id: 'present',
  name: 'Present',
  type: 'Normal',
  category: 'Physical',
  power: 60,
  accuracy: 90,
  pp: 15,
  description: 'A bomb that may restore HP.',
};

export default present;
