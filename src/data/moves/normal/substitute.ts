import { Move } from '../../../types/Pokemon';

const substitute: Move = {
  id: 'substitute',
  name: 'Substitute',
  type: 'Normal',
  category: 'Status',
  power: 0,
  accuracy: 100,
  pp: 10,
  description: 'Makes a decoy with 1/4 user\'s max HP.',
};

export default substitute;
