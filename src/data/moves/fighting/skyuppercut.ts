import { Move } from '../../../types/Pokemon';

const skyuppercut: Move = {
  id: 'skyuppercut',
  name: 'Sky Uppercut',
  type: 'Fighting',
  category: 'Physical',
  power: 85,
  accuracy: 90,
  pp: 15,
  description: 'An uppercut thrown as if leaping into the sky.',
};

export default skyuppercut;
