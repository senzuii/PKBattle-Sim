import { Move } from '../../../types/Pokemon';

const detect: Move = {
  id: 'detect',
  name: 'Detect',
  type: 'Fighting',
  category: 'Status',
  power: 0,
  accuracy: 100,
  pp: 5,
  description: 'Evades attack that turn. It may fail.',
};

export default detect;
