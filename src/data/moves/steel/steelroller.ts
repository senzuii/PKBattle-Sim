import { Move } from '../../../types/Pokemon';

const steelroller: Move = {
  id: 'steelroller',
  name: 'Steel Roller',
  type: 'Steel',
  category: 'Physical',
  power: 130,
  accuracy: 100,
  pp: 5,
  description: 'The user attacks while destroying the terrain. This move fails when the ground hasn’t turned into a terrain.',
};

export default steelroller;
